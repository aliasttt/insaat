# Force HTTPS and www redirects
RewriteEngine On

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Force non-www (canonical apex domain)
RewriteCond %{HTTP_HOST} ^www\.gezginustalar\.com$ [NC]
RewriteRule ^(.*)$ https://gezginustalar.com%{REQUEST_URI} [L,R=301]

# Remove trailing slashes (except for root)
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{THE_REQUEST} \s/+([^?\s]*?)/+(\?[^\s]*)?\s
RewriteRule ^ /%1%2 [R=301,L]

# Add trailing slash to directories
RewriteCond %{REQUEST_FILENAME} -d
RewriteCond %{REQUEST_URI} !/$
RewriteRule ^(.*)$ $1/ [R=301,L]

# Cache control for static assets
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/webp "access plus 1 month"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/pdf "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType application/x-javascript "access plus 1 month"
    ExpiresByType application/x-shockwave-flash "access plus 1 month"
    ExpiresByType image/x-icon "access plus 1 year"
    ExpiresDefault "access plus 2 days"
</IfModule>

# Gzip compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
</IfModule>

# Security headers
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options nosniff
    Header always set X-Frame-Options DENY
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
</IfModule>

# Custom error pages
ErrorDocument 404 /404.html

# Keep .html URLs as-is (no extension removal)

# Specific redirects for Turkish pages
RewriteRule ^hakkimizda/?$ /about.html [L]
RewriteRule ^hizmetler/?$ /services.html [L]
RewriteRule ^galeri/?$ /gallery.html [L]
RewriteRule ^iletisim/?$ /contact.html [L]
RewriteRule ^blog/?$ /blog.html [L]
RewriteRule ^projeler/?$ /projects.html [L]
RewriteRule ^teklif/?$ /quote.html [L]
RewriteRule ^gizlilik/?$ /privacy.html [L]
RewriteRule ^sartlar/?$ /terms.html [L]
